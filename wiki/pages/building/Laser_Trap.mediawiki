<!-- HHWIKI:GENERATED: remove this line to protect from regeneration -->
{{Nav}}

The '''Laser Trap''' is a trap in ''[[Human Host]]'', used for base defense against horde nights.

== Behavior ==

{| class="wikitable" style="font-size:0.9em;"
! Stat !! Value
|-
| Trap damage       || 300 per trigger
|-
| Trigger interval  || 0s
|-
| Hit-react force   || 0.1
|-
| Self-damage       || 2 per trigger
|-
| Hits allies       || No (zombies only)
|-
| Smashable         || No
|-
| Placement         || Dynamic (moving)
|-
| Beam duration     || 2s
|-
| Per-target cooldown || 1s
|}

== Placement ==

TBD: placement footprint and world-space clearance.

== Technical ==

Asset id: <code>BT_Laser_Trap</code>. Backed by class <code>Trap_Laser</code> (assembly <code>Trap</code>), derived from <code>Trap_Base</code>. Traps are registered via <code>Trap_Mgr._ins</code>.

== See also ==

* [[Building]]: trap placement in the build menu
* [[Combat]]: horde night defense strategy

[[Category:Traps]]
[[Category:Building]]